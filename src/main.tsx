import React, { useEffect, useMemo, useRef, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { supabase, UNO_FUNCTION_URL } from './lib/supabase';
import './styles.css';
import './design-refresh.css';
import { getWeeklyTheme } from './lib/theme';
import { GAMES } from './lib/cruise/catalog';
import { addToCart, loadCart, loadStore, placeOrder, setCartQty } from './lib/cruise/store';
import { initializePaystack, verifyPaystack } from './lib/cruise/payments';
import type { CartItem, Product, Variant } from './lib/cruise/store';
import { extractUnoState } from './lib/games/uno/response';
import { clearUnoSession, loadUnoSession, saveUnoSession } from './lib/games/uno/session';
import type { UnoCard } from './lib/games/uno/rules';
import { ClassicGameGate } from './components/ClassicSoloGame';
import { isMyTurn } from './lib/games/uno/view-model';
import { createLudoState, currentPlayer as ludoCurrentPlayer, legalPieceIds, movePiece as moveLudoPiece, rollDice as rollLudoDice, type LudoState } from './lib/games/ludo/rules';
import { createChessState, legalMoves as chessLegalMoves, makeMove as makeChessMove, type ChessColor, type ChessPieceKind, type ChessState } from './lib/games/chess/rules';
import { CruisePartyGame } from './components/CruisePartyGame';
import { MemberProfile } from './components/MemberProfile';
import { touchFeedback } from './lib/feedback';
import { SevenDaysCruise } from './components/SevenDaysCruise';
import { QuizRushGame } from './components/QuizRushGame';
import { StrategySoloGame } from './components/StrategySoloGame';
import { MusicHub } from './components/MusicHub';
import { BoardModeGate } from './components/BoardSoloGame';

type User={id:string;email?:string};
type Player={id:string;displayName:string;avatarUrl?:string|null;hand?:UnoCard[];handCount:number;ready:boolean};
type State={phase:'lobby'|'playing'|'finished';players:Player[];discardPile:UnoCard[];drawPileCount:number;currentPlayerIndex:number;currentColor:string;winnerId?:string;pendingUnoPlayerId?:string};
type Tab='home'|'games'|'music'|'rankings'|'community'|'merch'|'profile';

async function api(action:string,data:Record<string,unknown>={}){const {data:{session}}=await supabase.auth.getSession();const r=await fetch(UNO_FUNCTION_URL,{method:'POST',headers:{Authorization:`Bearer ${session?.access_token||''}`,'content-type':'application/json'},body:JSON.stringify({action,...data})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Request failed');return j;}
const cardLabel=(c:UnoCard)=>c.kind==='number'?String(c.value):({skip:'⊘',reverse:'↻',draw2:'+2',wild:'WILD',wild4:'+4'} as Record<string,string>)[c.kind];
const cardColor=(c:UnoCard)=>({red:'#ef3340',yellow:'#ffd400',green:'#16a34a',blue:'#2563eb',wild:'#151515'} as Record<string,string>)[c.color];
const naira=(n:number)=>`₦${n.toLocaleString('en-NG')}`;

function ThemeBadge({theme}:{theme:ReturnType<typeof getWeeklyTheme>}){return <span className="theme-badge">{theme.icon} {theme.shortLabel}</span>}
function Header({theme,onTab,onSignOut,preview}:{theme:ReturnType<typeof getWeeklyTheme>;onTab:(t:Tab)=>void;onSignOut:()=>void;preview?:boolean}){return <header className="app-header"><button className="brand" onClick={()=>onTab('home')} aria-label="Go to Cruise home">BIG CRUISE<span>〽</span></button><div className="header-right"><ThemeBadge theme={theme}/>{preview&&<span className="preview-pill">PREVIEW</span>}<button className="avatar-button" aria-label="Open member profile" onClick={()=>{touchFeedback('tap');onTab('profile')}}>FX</button><button className="ghost signout-button" onClick={onSignOut}>Sign out</button></div></header>}
function Home({theme,onTab}:{theme:ReturnType<typeof getWeeklyTheme>;onTab:(t:Tab)=>void}){const featured=GAMES.filter(g=>theme.featuredGames.includes(g.id));return <main className="page"><section className="today"><div className="eyebrow">TODAY'S CRUISE · {theme.weekday}</div><div className="theme-icon">{theme.icon}</div><h1>{theme.displayName}</h1><p>{theme.description}</p><div className="challenge"><b>🔥 TODAY'S CHALLENGE</b><span>{theme.challenge}</span></div><button className="primary big" onClick={()=>onTab('games')}>PLAY TODAY'S CRUISE →</button></section><SevenDaysCruise today={theme} onPlay={()=>onTab('games')}/><section><div className="section-head"><div><span className="eyebrow">FEATURED</span><h2>Games for today</h2></div><button className="link" onClick={()=>onTab('games')}>See all</button></div><div className="game-grid">{featured.map(g=><GameCard key={g.id} game={g} onPlay={()=>onTab('games')}/>)}</div></section></main>}
const gameArt=(id:string)=>id==='uno'||id==='ludo'?'card-table.jpg':id==='chess'||id==='othello'||id==='gomoku'||id==='mancala'?'strategy-board.jpg':id==='karaoke'?'mic-stage.jpg':id==='kahoot'?'quiz-screen.jpg':'party-room.jpg';
function GameCard({game,onPlay}:{game:(typeof GAMES)[number];onPlay:()=>void}){return <button className={`game-card ${game.status}`} onClick={onPlay}><span className="game-art" style={{'--game-accent':game.accent,'--game-art':`url(/assets/game-art/${gameArt(game.id)})`} as React.CSSProperties}><span>{game.icon}</span><i>{game.category||'Quick'}</i></span><span className="game-meta"><b>{game.name}</b><small>{game.players||'2+'} PLAYERS · {game.duration||'10 MIN'}</small></span><span className="game-arrow" aria-hidden="true">→</span></button>}

function UnoGame({theme,user}:{theme:ReturnType<typeof getWeeklyTheme>;user:User}){
  const [state,setState]=useState<State|null>(null);
  const [roomId,setRoomId]=useState('');
  const [code,setCode]=useState('');
  const [joinCode,setJoinCode]=useState('');
  const [error,setError]=useState('');
  const [busy,setBusy]=useState(false);
  const [selectedColor,setSelectedColor]=useState(false);
  const [reconnecting,setReconnecting]=useState(()=>!!loadUnoSession());

  const run=async(action:string,data:Record<string,unknown>={})=>{
    try{
      setBusy(true);setError('');
      const j=await api(action,data);
      const s=extractUnoState(j);
      if(s)setState(s as State);
      const nextRoomId=typeof j.roomId==='string'?j.roomId:'';
      const nextCode=typeof j.code==='string'?j.code:'';
      if(nextRoomId)setRoomId(nextRoomId);
      if(nextCode)setCode(nextCode);
      const persistId=nextRoomId||roomId;
      const persistCode=nextCode||code;
      if(persistId&&persistCode&&action!=='leave')saveUnoSession({roomId:persistId,code:persistCode});
      if(action==='join'&&j.roomId&&!s){
        const q=await api('state',{roomId:j.roomId});
        const ss=extractUnoState(q);
        if(ss)setState(ss as State);
        if(j.roomId&&(j.code||persistCode))saveUnoSession({roomId:String(j.roomId),code:String(j.code||persistCode)});
      }
      if(action==='leave'){clearUnoSession();setState(null);setRoomId('');setCode('');}
    }catch(e){setError(e instanceof Error?e.message:'Something went wrong')}
    finally{setBusy(false)}
  };

  // Rehydrate after hard refresh / reconnect
  useEffect(()=>{
    const saved=loadUnoSession();
    if(!saved){setReconnecting(false);return;}
    let cancelled=false;
    (async()=>{
      try{
        setBusy(true);setReconnecting(true);
        setRoomId(saved.roomId);setCode(saved.code);
        // Wait for auth token — page refresh restores Supabase session async
        let token='';
        for(let i=0;i<8&&!token&&!cancelled;i++){
          const {data:{session}}=await supabase.auth.getSession();
          token=session?.access_token||'';
          if(!token)await new Promise(r=>setTimeout(r,200));
        }
        if(cancelled)return;
        if(!token){
          // Keep sessionStorage; user may still be signing in
          setReconnecting(false);setBusy(false);
          return;
        }
        const j=await api('state',{roomId:saved.roomId});
        const s=extractUnoState(j);
        if(cancelled)return;
        if(s){
          setState(s as State);
          saveUnoSession(saved);
          if(typeof j.roomId==='string'&&j.roomId)setRoomId(j.roomId);
          if(typeof j.code==='string'&&j.code)setCode(j.code);
        }else{
          clearUnoSession();setRoomId('');setCode('');setState(null);
        }
      }catch(e){
        if(cancelled)return;
        const msg=e instanceof Error?e.message:'';
        // Only drop the saved room when the server says it is gone
        if(/not found|no such room|invalid room|expired|room does not exist/i.test(msg)){
          clearUnoSession();setRoomId('');setCode('');setState(null);
          setError('Could not reconnect to your room. Create or join again.');
        }else{
          // Transient (network / 401 race): keep session so background poll can recover
          setError(msg||'Reconnect failed. Stay on this tab — retrying…');
        }
      }finally{if(!cancelled){setBusy(false);setReconnecting(false);}}
    })();
    return()=>{cancelled=true;};
  },[user.id]);

  useEffect(()=>{if(!roomId)return;const id=setInterval(()=>void run('state',{roomId}),1800);return()=>clearInterval(id);},[roomId]);

  // Prefer reconnect UI while we still hold a roomId from session (poll may recover)
  if((reconnecting||(!!roomId&&!!loadUnoSession()))&&!state)return <section className="uno-entry"><div><span className="eyebrow">UNO ONLINE</span><h2>Reconnecting…</h2><p>Restoring your room after refresh.</p></div>{error&&<p className="error">{error}</p>}<button className="ghost" style={{marginTop:12}} onClick={()=>{clearUnoSession();setRoomId('');setCode('');setError('');}} disabled={busy}>Start over</button></section>;

  if(!state)return <section className="uno-entry"><div><span className="eyebrow">UNO ONLINE</span><h2>Pull up with your people.</h2><p>One BIG CRUISE login. Real rooms. Real players.</p></div><div className="actions"><button className="primary" onClick={()=>run('create')} disabled={busy}>Create room</button><div className="join"><input value={joinCode} onChange={e=>setJoinCode(e.target.value.toUpperCase())} placeholder="ROOM CODE" maxLength={6}/><button onClick={()=>run('join',{code:joinCode})} disabled={busy||joinCode.length<4}>Join</button></div></div>{error&&<p className="error">{error}</p>}</section>;

  const me=state.players.find(p=>p.id===user.id);const current=state.players[state.currentPlayerIndex];const myTurn=isMyTurn(state,user.id);const top=state.discardPile.at(-1);const hand=me?.hand||[];
  const play=async(c:UnoCard,chosenColor?:string)=>{if(c.color==='wild'&&!chosenColor){setSelectedColor(true);return}await run('play',{roomId,cardId:c.id,...(chosenColor?{chosenColor}:{})});setSelectedColor(false)};
  const leave=()=>{void run('leave',{roomId});clearUnoSession();setState(null);setRoomId('');setCode('');};

  return <section className="uno-table"><div className="room-head"><div><span className="eyebrow">ROOM {code}</span><h1>UNO <span className="yellow">〽️</span></h1><ThemeBadge theme={theme}/></div><button onClick={leave}>Leave room</button></div><div className="seats">{state.players.map((p,i)=><div className={`seat ${i===state.currentPlayerIndex&&state.phase==='playing'?'active':''}`} key={p.id}><span className="avatar">{p.displayName.slice(0,1).toUpperCase()}</span><div><b>{p.displayName}</b><small>{p.handCount} cards {i===state.currentPlayerIndex&&state.phase==='playing'?'• TURN':''}</small></div></div>)}</div><div className="table-center"><button className="draw-pile" disabled={!myTurn||busy} onClick={()=>run('draw',{roomId})}>{state.drawPileCount}<small>DRAW</small></button>{top&&<div className="big-card" style={{background:cardColor(top)}}>{cardLabel(top)}</div>}<div className="status">{state.phase==='lobby'?'WAITING FOR PLAYERS':state.phase==='finished'?`${state.players.find(p=>p.id===state.winnerId)?.displayName||'Someone'} wins!`:myTurn?'YOUR TURN':`${current?.displayName||'Opponent'}'s turn`}<small>{state.phase==='lobby'?`${state.players.length}/4 PLAYERS`:`COLOR: ${state.currentColor.toUpperCase()}`}</small></div></div><div className="hand">{hand.map(c=><button key={c.id} className="uno-card" style={{background:cardColor(c)}} disabled={!myTurn||busy} onClick={()=>void play(c)}>{cardLabel(c)}</button>)}</div><div className="controls">{state.pendingUnoPlayerId===user.id&&<button className="primary" onClick={()=>run('call_uno',{roomId})}>UNO! 🚨</button>}{state.phase==='lobby'&&<button className="primary" onClick={()=>run('ready',{roomId,ready:true})}>Ready</button>}{state.phase==='lobby'&&state.players.length>=2&&state.players.every(p=>p.ready)&&<button onClick={()=>run('start',{roomId})}>Start game</button>}{state.phase==='finished'&&<button className="primary" onClick={()=>run('rematch',{roomId})}>Rematch</button>}</div>{error&&<p className="error">{error}</p>}{selectedColor&&<div className="modal-backdrop"><div className="modal"><h2>Pick your colour</h2><p>BIG CRUISE wild card.</p><div className="color-picks">{['red','yellow','green','blue'].map(c=><button key={c} className="color-pick" style={{background:c==='yellow'?'#ffd400':c}} onClick={()=>{const card=hand.find(x=>x.color==='wild');if(card)void play(card,c)}}>{c}</button>)}</div><button onClick={()=>setSelectedColor(false)}>Cancel</button></div></div>}</section>;
}

function LudoGame({user}:{user:User}){
  const [state,setState]=useState<LudoState|null>(null);const [mode,setMode]=useState<'solo'|'bots'|'online'|null>(null);const [code,setCode]=useState('');const [joinCode,setJoinCode]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);
  const apiLudo=async(action:string,data:Record<string,unknown>={})=>{const {data:{session}}=await supabase.auth.getSession();const r=await fetch(`${UNO_FUNCTION_URL.replace('/uno','/ludo')}`,{method:'POST',headers:{Authorization:`Bearer ${session?.access_token||''}`,'content-type':'application/json'},body:JSON.stringify({action,code,...data})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Ludo request failed');return j};
  const botTurn=(next:LudoState)=>{let current=next;let guard=0;while(current.status==='playing'&&current.players[current.currentPlayerIndex]?.id.startsWith('bot-')&&guard<30){const bot=current.players[current.currentPlayerIndex];rollLudoDice(current,bot.id);const ids=legalPieceIds(current,bot.id);if(ids.length)moveLudoPiece(current,bot.id,ids[Math.floor(Math.random()*ids.length)]);guard+=1}setState({...current})};
  const startLocal=(kind:'solo'|'bots')=>{const players=kind==='solo'?[{id:user.id,displayName:'You',ready:true}]:[{id:user.id,displayName:'You',ready:true},{id:'bot-red',displayName:'Cruise Bot 1',ready:true},{id:'bot-yellow',displayName:'Cruise Bot 2',ready:true},{id:'bot-blue',displayName:'Cruise Bot 3',ready:true}];setMode(kind);setState(createLudoState({matchId:`local-${Date.now()}`,roomId:'LOCAL',players,status:'playing'}));setError('')};
  const run=async(action:string,data:Record<string,unknown>={})=>{try{setBusy(true);setError('');if(mode==='solo'||mode==='bots'){if(!state)return;if(action==='roll'){rollLudoDice(state,user.id);if(mode==='bots'&&state.status==='playing'&&state.players[state.currentPlayerIndex]?.id.startsWith('bot-'))botTurn(state);else setState({...state});return}if(action==='move'){moveLudoPiece(state,user.id,String(data.pieceId));if(mode==='bots'&&state.status==='playing')botTurn(state);else setState({...state});return}if(action==='rematch'){startLocal(mode);return}if(action==='leave'){setState(null);setMode(null);return}return}const j=await apiLudo(action,data);if(j.code)setCode(j.code);if(j.phase)setState(j as LudoState);if(action==='join'&&!j.phase){const s=await apiLudo('state',{code:j.code||code});setState(s as LudoState)}if(action==='leave'){setState(null);setCode('');setMode(null)}}catch(e){setError(e instanceof Error?e.message:'Ludo request failed')}finally{setBusy(false)}};
  useEffect(()=>{if(!code||mode!=='online')return;const id=setInterval(()=>void run('state'),1800);return()=>clearInterval(id)},[code,mode]);
  const cellFor=(row:number,col:number)=>{const path:[number,number][]=[];for(let c=1;c<=13;c++)path.push([6,c]);for(let r=5;r>=1;r--)path.push([r,13]);for(let r=0;r<=5;r++)path.push([r,8]);for(let c=9;c<=13;c++)path.push([6,c]);for(let r=7;r<=13;r++)path.push([r,8]);for(let c=9;c<=13;c++)path.push([8,c]);for(let r=9;r<=13;r++)path.push([r,8]);return path.findIndex(([r,c])=>r===row&&c===col)};
  if(!state)return <section className="ludo-shell"><span className="eyebrow">LUDO ARCADE</span><h1>Race your pieces home.</h1><p className="muted">Play solo, challenge Cruise Bots, or create a real room with your people.</p><div className="mode-grid"><button className="primary" onClick={()=>startLocal('solo')}>Solo practice<small>No opponent · learn the board</small></button><button onClick={()=>startLocal('bots')}>Play Cruise Bots<small>Three bot opponents</small></button><button onClick={()=>{setMode('online');setError('')}}>Create online room<small>Invite real players</small></button><div className="join"><input value={joinCode} onChange={e=>setJoinCode(e.target.value.toUpperCase())} placeholder="ROOM CODE" maxLength={4}/><button onClick={()=>{setMode('online');void run('join',{code:joinCode})}} disabled={busy||joinCode.length!==4}>Join room</button></div></div>{mode==='online'&&<button className="primary" onClick={()=>void run('create')} disabled={busy}>Create room</button>}{error&&<p className="error" role="alert">{error}</p>}</section>;
  const current=ludoCurrentPlayer(state);const local=mode==='solo'||mode==='bots';const legal=legalPieceIds(state,user.id);const boardPieces=state.players.flatMap((player,playerIndex)=>player.pieces.map(piece=>({player,playerIndex,piece})));const runAction=(action:string,data:Record<string,unknown>={})=>void run(action,data);
  return <section className="ludo-shell"><div className="room-head"><div><span className="eyebrow">{local?'LOCAL MATCH':`ROOM ${code}`} · LUDO</span><h1>Ludo <span className="yellow">〽️</span></h1><p className="muted">{state.status==='finished'?`${state.players.find(p=>p.id===state.winnerId)?.displayName||'Someone'} wins!`:current.id===user.id?'Your turn':'Opponent turn'}</p></div><button onClick={()=>runAction('leave')}>Exit</button></div><div className="ludo-status"><div><b>{state.phase==='await_roll'?'Roll the dice':`Choose a piece · ${state.dice}`}</b><small>{current.displayName}'s turn</small></div><button className="primary dice-button" disabled={busy||state.status==='finished'||state.phase!=='await_roll'||current.id!==user.id} onClick={()=>runAction('roll')}>{state.dice??'ROLL'}<small>DICE</small></button></div><div className="ludo-board" aria-label="Ludo board">{Array.from({length:225},(_,index)=>{const row=Math.floor(index/15),col=index%15,track=cellFor(row,col);const piece=track===-1?undefined:boardPieces.find(({piece:p,playerIndex})=>{const position=p.steps<0?null:p.steps>=52?null:(playerIndex*13+p.steps)%52;return position===track});return <div key={index} className={`ludo-cell ${row<6&&col<6?'base-red':''} ${row<6&&col>8?'base-green':''} ${row>8&&col<6?'base-blue':''} ${row>8&&col>8?'base-yellow':''} ${track>=0?'track':''} ${piece?'occupied':''}`} style={piece?{['--piece-color' as string]:piece.player.color}:undefined}>{piece&&<button aria-label={`${piece.player.displayName} piece`} className="ludo-piece" disabled={!legal.includes(piece.piece.id)} onClick={()=>runAction('move',{pieceId:piece.piece.id})}>{piece.playerIndex+1}</button>}</div>})}</div>{state.status==='finished'&&<div className="controls"><button className="primary" onClick={()=>runAction('rematch')} disabled={busy}>Rematch</button></div>}{error&&<p className="error" role="alert">{error}</p>}</section>
}
const CHESS_GLYPHS:Record<ChessColor,Record<ChessPieceKind,string>>={white:{king:'♔',queen:'♕',rook:'♖',bishop:'♗',knight:'♘',pawn:'♙'},black:{king:'♚',queen:'♛',rook:'♜',bishop:'♝',knight:'♞',pawn:'♟'}};
type ChessServerState=ChessState & {status:'lobby'|'playing'|'finished';players:{id:string;displayName:string;color:ChessColor;ready:boolean}[]};
function ChessGame({user}:{user:User}){
  const [state,setState]=useState<ChessServerState|null>(null);const [code,setCode]=useState('');const [joinCode,setJoinCode]=useState('');const [selected,setSelected]=useState<{row:number;col:number}|null>(null);const [error,setError]=useState('');const [busy,setBusy]=useState(false);
  const apiChess=async(action:string,data:Record<string,unknown>={})=>{const {data:{session}}=await supabase.auth.getSession();const r=await fetch(`${UNO_FUNCTION_URL.replace('/uno','/chess')}`,{method:'POST',headers:{Authorization:`Bearer ${session?.access_token||''}`,'content-type':'application/json'},body:JSON.stringify({action,code,...data})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Chess request failed');return j};
  const run=async(action:string,data:Record<string,unknown>={})=>{try{setBusy(true);setError('');const j=await apiChess(action,data);if(j.code)setCode(j.code);if(j.board)setState(j as ChessServerState);if(action==='join'&&!j.board){const s=await apiChess('state',{code:j.code||code});setState(s as ChessServerState)}if(action==='leave'){setState(null);setCode('')}}catch(e){setError(e instanceof Error?e.message:'Chess request failed')}finally{setBusy(false)}};
  useEffect(()=>{if(!code)return;const id=setInterval(()=>void run('state'),1800);return()=>clearInterval(id)},[code]);
  const moves=selected&&state?.status==='playing'?chessLegalMoves(state,selected):[];const me=state?.players.find(p=>p.id===user.id);const choose=(row:number,col:number)=>{if(!state||state.status!=='playing')return;const target=moves.find(m=>m.to.row===row&&m.to.col===col);if(target&&selected){void run('move',{move:{...target,promotion:target.promotion||'queen'}});setSelected(null);return}const piece=state.board[row][col];if(piece&&piece.color===me?.color&&piece.color===state.turn){setSelected({row,col});return}setSelected(null)};
  if(!state)return <section className="chess-shell"><span className="eyebrow">CHESS ONLINE</span><h1>Think fast. Play sharp.</h1><p className="muted">Authenticated rooms. Server-validated moves. Real match results.</p><div className="actions"><button className="primary" onClick={()=>void run('create')} disabled={busy}>Create room</button><div className="join"><input value={joinCode} onChange={e=>setJoinCode(e.target.value.toUpperCase())} placeholder="ROOM CODE" maxLength={4}/><button onClick={()=>void run('join',{code:joinCode})} disabled={busy||joinCode.length!==4}>Join</button></div></div>{error&&<p className="error" role="alert">{error}</p>}</section>;
  return <section className="chess-shell"><div className="room-head"><div><span className="eyebrow">ROOM {code} · CHESS</span><h1>Chess <span className="yellow">〽️</span></h1><p className="muted">{state.status==='lobby'?`${state.players.length}/2 players · ready up`:state.result==='playing'?`${state.turn==='white'?'White':'Black'} to move`:state.result.replaceAll('_',' ')}</p></div><button onClick={()=>void run('leave')}>Exit</button></div>{state.status==='lobby'&&<div className="controls">{state.players.map(p=><span className="theme-badge" key={p.id}>{p.displayName} · {p.ready?'READY':'NOT READY'}</span>)}<button className="primary" onClick={()=>void run('ready',{ready:true})} disabled={busy}>Ready</button>{state.players.length===2&&state.players.every(p=>p.ready)&&<button onClick={()=>void run('start')} disabled={busy}>Start match</button>}</div>}{state.status!=='lobby'&&<><div className="chess-board" role="grid" aria-label="Chess board">{state.board.map((row,rowIndex)=>row.map((piece,col)=>{const active=selected?.row===rowIndex&&selected.col===col;const legal=moves.some(m=>m.to.row===rowIndex&&m.to.col===col);return <button role="gridcell" aria-label={`${String.fromCharCode(97+col)}${8-rowIndex}${piece?` ${piece.color} ${piece.kind}`:''}`} key={`${rowIndex}-${col}`} className={`chess-square ${(rowIndex+col)%2?'dark':'light'} ${active?'selected':''} ${legal?'legal':''}`} onClick={()=>choose(rowIndex,col)} disabled={state.result!=='playing'}>{piece&&<span className={`chess-piece ${piece.color}`}>{CHESS_GLYPHS[piece.color][piece.kind]}</span>}</button>}))}</div><div className="chess-help"><span>{state.result==='playing'?'Tap a piece, then a highlighted square.':'MATCH COMPLETE'}</span><span>{state.result!=='playing'&&<button className="primary" onClick={()=>void run('rematch')} disabled={busy}>Rematch</button>}</span></div></>}{error&&<p className="error" role="alert">{error}</p>}</section>;
}

type OnmugaGameName='tictactoe'|'connect4'|'draw';
type OnmugaPlayer={id:string;displayName:string;role?:string;isDrawer?:boolean};
type OnmugaState={game:OnmugaGameName;board?:((string|null)[]|((string|null)[])[]) ;turn?:string;winner?:string|null;round?:number;drawerId?:string|null;word?:string|null;roundActive?:boolean;strokes?:{x:number;y:number;color:string;size:number;tool:'begin'|'move'|'end'|'clear'}[];guesses?:{playerId:string;text:string}[];winnerId?:string|null;sessionVersion?:number;players:OnmugaPlayer[]};
function OnmugaGame({game,user}:{game:OnmugaGameName;user:User}){
 const [state,setState]=useState<OnmugaState|null>(null);const [code,setCode]=useState('');const [joinCode,setJoinCode]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const [guess,setGuess]=useState('');const canvasRef=useRef<HTMLCanvasElement|null>(null);const drawingRef=useRef(false);const lastRef=useRef<{x:number;y:number}|null>(null);
 const api=async(action:string,data:Record<string,unknown>={})=>{const {data:{session}}=await supabase.auth.getSession();const r=await fetch(`${UNO_FUNCTION_URL.replace('/uno','/onmuga')}`,{method:'POST',headers:{Authorization:`Bearer ${session?.access_token||''}`,'content-type':'application/json'},body:JSON.stringify({action,code,game,...data})});const j=await r.json();if(!r.ok)throw Error(j.error||'Game request failed');return j};
 const run=async(action:string,data:Record<string,unknown>={})=>{try{setBusy(true);setError('');const j=await api(action,data);if(j.code)setCode(j.code);if(j.game&&('board' in j||'round' in j)){setState(j as OnmugaState);if(game==='draw'&&action==='state'&&!j.roundActive&&j.players?.[0]?.id===user.id){const started=await api('new-round');setState(started as OnmugaState)}}else if((action==='create'||action==='join')&&j.code){const next=await api('state',{code:String(j.code)});setState(next as OnmugaState)}}catch(e){setError(e instanceof Error?e.message:'Game request failed')}finally{setBusy(false)}};
 useEffect(()=>{if(!code)return;let cancelled=false;const refresh=async()=>{try{const latest=await api('state');if(!cancelled&&latest.game)setState(latest as OnmugaState)}catch{}};const channel=supabase.channel(`onmuga:${game}:${code}`).on('postgres_changes',{event:'UPDATE',schema:'public',table:'game_sessions',filter:`room_code=eq.${code}`},payload=>{const row=payload.new as {state?:unknown;version?:number};const incoming=row.state as OnmugaState|undefined;if(!incoming?.game)return;setState(previous=>{const version=Number(row.version||0);if(previous?.sessionVersion&&version<=previous.sessionVersion)return previous;return{...incoming,players:previous?.players||[],sessionVersion:version}})}).on('postgres_changes',{event:'*',schema:'public',table:'game_players',filter:`room_code=eq.${code}`},()=>void refresh()).subscribe();void refresh();return()=>{cancelled=true;void supabase.removeChannel(channel)}},[code,game]);
 useEffect(()=>{const canvas=canvasRef.current;if(!canvas||!state?.strokes)return;const ctx=canvas.getContext('2d');if(!ctx)return;ctx.clearRect(0,0,canvas.width,canvas.height);ctx.lineCap='round';ctx.lineJoin='round';for(const stroke of state.strokes){if(stroke.tool==='clear'){ctx.clearRect(0,0,canvas.width,canvas.height);continue}ctx.strokeStyle=stroke.color;ctx.lineWidth=stroke.size;if(stroke.tool==='begin'){ctx.beginPath();ctx.moveTo(stroke.x,stroke.y)}else if(stroke.tool==='move'){ctx.lineTo(stroke.x,stroke.y);ctx.stroke()}else if(stroke.tool==='end'){ctx.closePath()}}},[state?.strokes]);
 const enter=async(action:string,data:Record<string,unknown>={})=>{await run(action,data);};
 if(!state)return <section className="onmuga-shell"><span className="eyebrow">BIG CRUISE ARCADE · {game==='tictactoe'?'TIC-TAC-TOE':game==='connect4'?'CONNECT FOUR':'DRAW IT OUT'}</span><h1>{game==='tictactoe'?'Quick marks. Big energy.':game==='connect4'?'Drop four. Own the board.':'Draw loud. Guess fast.'}</h1><p className="muted">Authenticated rooms, server-checked moves, and mobile-first play.</p><div className="actions"><button className="primary" onClick={()=>void enter('create')} disabled={busy}>Create room</button><div className="join"><input value={joinCode} onChange={e=>setJoinCode(e.target.value.toUpperCase())} placeholder="ROOM CODE" maxLength={6}/><button onClick={()=>{setCode(joinCode);void run('join',{code:joinCode})}} disabled={busy||joinCode.length<4}>Join</button></div></div>{error&&<p className="error" role="alert">{error}</p>}</section>;
 const me=state.players.find(p=>p.id===user.id);const leave=()=>{void run('leave');setState(null);setCode('')};
 if(game==='tictactoe'){const board=(state.board||[]) as (string|null)[];return <section className="onmuga-shell"><div className="room-head"><div><span className="eyebrow">ROOM {code} · TIC-TAC-TOE</span><h1>Three in a row.</h1><p className="muted">{state.winner?state.winner==='draw'?'DRAW GAME':`${state.winner} WINS`:`${state.turn===me?.role?'YOUR TURN':'WAITING'} · YOU ARE ${me?.role||'—'}`}</p></div><button onClick={leave}>Exit</button></div><div className="player-strip">{state.players.map(p=><span className={p.role===state.turn?'active':''} key={p.id}>{p.role} · {p.displayName}</span>)}</div><div className="ttt-board">{board.map((v,i)=><button key={i} aria-label={`Cell ${i+1}${v?` ${v}`:''}`} disabled={Boolean(v)||Boolean(state.winner)||state.turn!==me?.role} onClick={()=>void run('move',{index:i})}>{v||''}</button>)}</div><div className="controls">{state.winner&&<button className="primary" onClick={()=>void run('replay')}>Replay</button>}</div>{error&&<p className="error" role="alert">{error}</p>}</section>}
 if(game==='connect4'){const board=(state.board||[]) as (string|null)[][];return <section className="onmuga-shell"><div className="room-head"><div><span className="eyebrow">ROOM {code} · CONNECT FOUR</span><h1>Drop four.</h1><p className="muted">{state.winner?state.winner==='draw'?'DRAW GAME':`${state.winner} WINS`:`${state.turn===me?.role?'YOUR TURN':'WAITING'} · YOU ARE ${me?.role||'—'}`}</p></div><button onClick={leave}>Exit</button></div><div className="player-strip">{state.players.map(p=><span className={p.role===state.turn?'active':''} key={p.id}>{p.role} · {p.displayName}</span>)}</div><div className="connect-board"><div className="connect-drops">{Array.from({length:7},(_,c)=><button key={c} aria-label={`Drop in column ${c+1}`} disabled={Boolean(state.winner)||state.turn!==me?.role} onClick={()=>void run('move',{column:c})}>▼</button>)}</div>{board.map((row,r)=>row.map((v,c)=><div key={`${r}-${c}`} className={`connect-slot ${v==='R'?'red':''} ${v==='Y'?'gold':''}`}>{v&&<span>{v}</span>}</div>))}</div><div className="controls">{state.winner&&<button className="primary" onClick={()=>void run('replay')}>Replay</button>}</div>{error&&<p className="error" role="alert">{error}</p>}</section>}
 const drawer=state.drawerId===user.id;const point=(e:React.PointerEvent<HTMLCanvasElement>)=>{const c=canvasRef.current!;const r=c.getBoundingClientRect();return{x:(e.clientX-r.left)*(c.width/r.width),y:(e.clientY-r.top)*(c.height/r.height)}};const stroke=(tool:'begin'|'move'|'end'|'clear',pt={x:0,y:0})=>void run('draw',{stroke:{...pt,color:'#ffd400',size:5,tool}});return <section className="onmuga-shell"><div className="room-head"><div><span className="eyebrow">ROOM {code} · DRAW IT OUT</span><h1>Draw loud. Guess fast.</h1><p className="muted">{state.roundActive?(drawer?`YOUR WORD: ${state.word||'—'}`:'Guess what you see'):'Waiting for the next round'}</p></div><button onClick={leave}>Exit</button></div><div className="player-strip">{state.players.map(p=><span className={p.id===state.drawerId?'active':''} key={p.id}>{p.displayName}{p.id===state.drawerId?' · DRAWER':''}</span>)}</div><canvas ref={canvasRef} width={700} height={420} className="draw-canvas" onPointerDown={e=>{if(!drawer||!state.roundActive)return;drawingRef.current=true;lastRef.current=point(e);e.currentTarget.setPointerCapture(e.pointerId);stroke('begin',lastRef.current)}} onPointerMove={e=>{if(!drawingRef.current||!lastRef.current)return;lastRef.current=point(e);stroke('move',lastRef.current)}} onPointerUp={()=>{if(drawingRef.current)stroke('end');drawingRef.current=false;lastRef.current=null}}/><div className="draw-toolbar"><span>{state.roundActive?`ROUND ${state.round||1}`:'WAITING'}</span><span>{state.guesses?.length||0} guesses</span>{state.players[0]?.id===user.id&&<button onClick={()=>void run('new-round')}>New round</button>}</div><div className="guess-row"><input value={guess} onChange={e=>setGuess(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&guess.trim()){void run('guess',{text:guess});setGuess('')}}} placeholder={drawer?'You are drawing':'Type a guess and press Enter'} disabled={drawer||!state.roundActive}/></div><div className="guess-feed">{state.guesses?.map((g,i)=><div key={i}>{g.text}</div>)}</div>{state.winnerId&&<div className="result-banner">Round winner · {state.players.find(p=>p.id===state.winnerId)?.displayName||'Cruiser'}</div>}{error&&<p className="error" role="alert">{error}</p>}</section>
}

function Games({theme,user}:{theme:ReturnType<typeof getWeeklyTheme>;user:User}){const [selected,setSelected]=useState('catalogue');const game=GAMES.find(g=>g.id===selected);if(selected==='catalogue')return <main className="page"><div className="section-head"><div><span className="eyebrow">🎮 BIG CRUISE ARCADE</span><h1>Choose your game.</h1><p className="muted">{theme.icon} {theme.displayName} · pick a game and get straight to the action.</p><img className="arcade-icon" src="/assets/game-icon-realistic.jpg" alt="Realistic BIG CRUISE game icon showing a gold cruise mark, playing card, die, chess knight and microphone"/></div></div><div className="game-grid all">{GAMES.map(g=><GameCard key={g.id} game={g} onPlay={()=>setSelected(g.id)}/>)}</div></main>;if(selected==='ludo')return <main className="page"><button className="link" onClick={()=>setSelected('catalogue')}>← Back to games</button><ClassicGameGate game="ludo" user={user} multiplayer={<LudoGame user={user}/>}/></main>;if(selected==='chess')return <main className="page"><button className="link" onClick={()=>setSelected('catalogue')}>← Back to games</button><ClassicGameGate game="chess" user={user} multiplayer={<ChessGame user={user}/>}/></main>;if(selected==='uno')return <main className="page"><button className="link" onClick={()=>setSelected('catalogue')}>← Back to games</button><ClassicGameGate game="uno" user={user} multiplayer={<UnoGame theme={theme} user={user}/>}/></main>;if(selected==='tictactoe')return <main className="page"><button className="link" onClick={()=>setSelected('catalogue')}>← Back to games</button><BoardModeGate game="tictactoe" user={user} multiplayer={<OnmugaGame game="tictactoe" user={user}/>}/></main>;if(selected==='connect4')return <main className="page"><button className="link" onClick={()=>setSelected('catalogue')}>← Back to games</button><BoardModeGate game="connect4" user={user} multiplayer={<OnmugaGame game="connect4" user={user}/>}/></main>;if(selected==='draw-it-out')return <main className="page"><button className="link" onClick={()=>setSelected('catalogue')}>← Back to games</button><ClassicGameGate game="draw-it-out" user={user} multiplayer={<OnmugaGame game="draw" user={user}/>}/></main>;if(selected==='kahoot')return <main className="page"><button className="link" onClick={()=>setSelected('catalogue')}>← Back to games</button><CruisePartyGame game="kahoot" user={user}/></main>;if(['othello','gomoku','mancala','word-hunt'].includes(selected))return <main className="page"><button className="link" onClick={()=>setSelected('catalogue')}>← Back to games</button><StrategySoloGame game={selected as 'othello'|'gomoku'|'mancala'|'word-hunt'} onBack={()=>setSelected('catalogue')}/></main>;if(game&&['werewolf','codenames','word-guess','karaoke','truth-or-dare'].includes(game.id))return <main className="page"><button className="link" onClick={()=>setSelected('catalogue')}>← Back to games</button><CruisePartyGame game={game.id as 'werewolf'|'codenames'|'word-guess'|'karaoke'|'truth-or-dare'} user={user}/></main>;return <main className="page"><button className="link" onClick={()=>setSelected('catalogue')}>← Back to games</button><section className="coming"><div className="coming-icon">{game?.icon}</div><h2>{game?.name} is in production.</h2><p>This game slot is reserved for the full rules engine. No fake gameplay is being presented.</p></section></main>}
function Merch(){const [products,setProducts]=useState<Product[]>([]);const [variants,setVariants]=useState<Variant[]>([]);const [cart,setCart]=useState<CartItem[]>([]);const [selected,setSelected]=useState<Product|null>(null);const [size,setSize]=useState('');const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');const [checkout,setCheckout]=useState(false);const [form,setForm]=useState({name:'',phone:'',address:'',city:'Lagos',email:''});
 const refresh=async()=>{const [store,bag]=await Promise.all([loadStore(),loadCart()]);setProducts(store.products);setVariants(store.variants);setCart(bag)};
 useEffect(()=>{void refresh().catch(e=>setMessage(e instanceof Error?e.message:'Store unavailable'))},[]);
 useEffect(()=>{const params=new URLSearchParams(window.location.search);if(params.get('payment')!=='complete')return;const reference=params.get('reference')||undefined;const verify=async()=>{try{setBusy(true);const orderId=reference;if(!orderId)throw new Error('Missing payment reference.');const result=await verifyPaystack(orderId,reference);setMessage(result.status==='paid'?`Payment confirmed 🎉 Order ${orderId} is paid.`:`Payment status: ${result.status||'pending'}.`);window.history.replaceState({},'',window.location.pathname)}catch(e){setMessage(e instanceof Error?e.message:'Could not verify payment.')}finally{setBusy(false)}};void verify()},[]);
 const availableSizes=(p:Product)=>variants.filter(v=>v.sku===p.sku&&v.stock>0);const cartTotal=cart.reduce((sum,i)=>{const p=products.find(x=>x.sku===i.sku);return sum+(p?.price_ngn||0)*i.qty},0);
 const add=async()=>{if(!selected||!size)return;try{setBusy(true);setMessage('');setCart(await addToCart(selected.sku,size));setSelected(null);setSize('')}catch(e){setMessage(e instanceof Error?e.message:'Could not add to bag')}finally{setBusy(false)}};
 const checkoutOrder=async(e:React.FormEvent)=>{e.preventDefault();try{setBusy(true);setMessage('');const result=await placeOrder(form);const payment=await initializePaystack(result.orderId);setCheckout(false);if(!payment.authorizationUrl)throw new Error('Payment checkout URL was not returned.');window.location.assign(payment.authorizationUrl)}catch(e){setMessage(e instanceof Error?e.message:'Could not start payment')}finally{setBusy(false)}};
 return <main className="page"><section className="merch-hero"><span className="eyebrow">🛍️ BIG CRUISE MERCH</span><h1>Wear the Cruise.</h1><p>Live catalogue and inventory from BIG CRUISE. Choose your size, add to bag and pay securely with Paystack.</p></section>{message&&<div className="store-message">{message}</div>}<div className="store-toolbar"><span>{products.length} products</span><button className="primary" onClick={()=>setCheckout(true)} disabled={!cart.length||busy}>Bag ({cart.reduce((n,i)=>n+i.qty,0)}) · {naira(cartTotal)}</button></div><div className="product-grid">{products.map(p=>{const stock=availableSizes(p);return <article className="product" key={p.sku}><div className="product-art">〽️</div><div><b>{p.name}</b><span>{naira(p.price_ngn)}</span><small>{p.collection} · {p.status==='preorder'?'PRE-ORDER':p.status.toUpperCase()}</small><p>{p.description}</p></div><button className="primary" disabled={!stock.length||p.status!=='preorder'||busy} onClick={()=>{setSelected(p);setSize(stock[0]?.size||'')}}>{stock.length?'Choose size':'Sold out'}</button></article>})}</div>{cart.length>0&&<section className="cart-panel"><div className="section-head"><div><span className="eyebrow">YOUR BAG</span><h2>Ready to Cruise?</h2></div><b>{naira(cartTotal)}</b></div>{cart.map(i=>{const p=products.find(x=>x.sku===i.sku);return <div className="cart-row" key={i.id}><span>{p?.name||i.sku} · {i.size}</span><div><button onClick={async()=>setCart(await setCartQty(i.sku,i.size,Math.max(0,i.qty-1)))} disabled={busy}>−</button><b>{i.qty}</b><button onClick={async()=>setCart(await setCartQty(i.sku,i.size,i.qty+1))} disabled={busy}>+</button></div></div>})}<button className="primary big" onClick={()=>setCheckout(true)} disabled={busy}>Checkout →</button></section>}{selected&&<div className="modal-backdrop"><div className="modal"><h2>{selected.name}</h2><p>{naira(selected.price_ngn)} · Select your size.</p><div className="size-grid">{availableSizes(selected).map(v=><button className={size===v.size?'selected':''} key={v.id} onClick={()=>setSize(v.size)}>{v.size}<small>{v.stock} left</small></button>)}</div><button className="primary" disabled={!size||busy} onClick={()=>void add()}>Add to bag</button><button onClick={()=>setSelected(null)}>Cancel</button></div></div>}{checkout&&<div className="modal-backdrop"><form className="modal checkout" onSubmit={checkoutOrder}><h2>Checkout</h2><p>Total: <b>{naira(cartTotal)}</b></p>{(['name','phone','email','city'] as const).map(k=><input key={k} required={k!=='email'} placeholder={k[0].toUpperCase()+k.slice(1)} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/>)}<textarea required placeholder="Delivery address" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/><p className="muted">You’ll be redirected to Paystack to complete payment. BIG CRUISE only marks an order paid after server-side verification.</p><button className="primary" disabled={busy||!cart.length} type="submit">{busy?'Opening secure payment…':'Pay securely with Paystack'}</button><button type="button" onClick={()=>setCheckout(false)}>Cancel</button></form></div>}</main>}
function Simple({title,body}:{title:string;body:string}){return <main className="page"><section className="empty-page"><span className="eyebrow">BIG CRUISE〽️</span><h1>{title}</h1><p>{body}</p></section></main>}

function App(){
  const theme=useMemo(()=>getWeeklyTheme(new Date()),[]);
  const preview=useMemo(()=>new URLSearchParams(window.location.search).get('preview')==='1',[ ]);
  const [user,setUser]=useState<User|null>(null);
  const [tab,setTab]=useState<Tab>(()=>{
    if(new URLSearchParams(window.location.search).get('payment')==='complete')return 'merch';
    if(loadUnoSession())return 'games';
    return 'home';
  });
  useEffect(()=>{supabase.auth.getSession().then(({data})=>{if(data.session)setUser({id:data.session.user.id,email:data.session.user.email})});const s=supabase.auth.onAuthStateChange((_e,session)=>setUser(session?{id:session.user.id,email:session.user.email}:null));return()=>s.data.subscription.unsubscribe()},[]);
  useEffect(()=>{const onTouch=(event:PointerEvent)=>{const target=event.target as HTMLElement|null;if(target?.closest('button,a,input,select,textarea'))touchFeedback('tap')};document.addEventListener('pointerdown',onTouch,{passive:true});return()=>document.removeEventListener('pointerdown',onTouch)},[]);
  if(!user&&!preview)return <main className="auth"><div className="auth-orbit" aria-hidden="true"><span>🃏</span><span>🎲</span><span>♟️</span></div><div className="logo">BIG CRUISE<span>〽️</span></div><ThemeBadge theme={theme}/><h1 className="auth-title">Pull up. Play loud.</h1><p className="auth-subtitle">Your seat is waiting. Sign in to enter the games, rooms and weekly challenges.</p><Login onUser={setUser}/><a className="preview-link" href="/?preview=1">Preview 7 Days of Cruise without signing in</a></main>;
  const activeUser=user||{id:'preview-user',email:'preview@bigcruise.local'};
  const signOut=()=>{if(preview){window.location.href='/';return}void supabase.auth.signOut()};
  return <div className="app" style={{'--day-accent':theme.accent,'--day-accent-alt':theme.accentAlt,'--day-accent-soft':theme.accentSoft} as React.CSSProperties}>{preview&&<div className="preview-banner" role="status">Read-only preview mode · no account or game results are saved <a href="/">Return to login</a></div>}<Header theme={theme} onTab={setTab} onSignOut={signOut} preview={preview}/>{tab==='home'&&<Home theme={theme} onTab={setTab}/>}{tab==='games'&&<Games theme={theme} user={activeUser}/>}{tab==='music'&&<MusicHub/>}{tab==='merch'&&!preview&&<Merch/>}{tab==='rankings'&&<Simple title="Cruise Rankings" body="Daily, weekly and all-time rankings will aggregate results from every in-app game and weekly challenge."/>}{tab==='community'&&<Simple title="The Cruise" body="Your community layer is where game results, challenges, profiles and weekly culture come back together."/>}{tab==='profile'&&<MemberProfile email={activeUser.email}/>}<nav className="bottom-nav" aria-label="Primary navigation">{([['home','CR','Cruise'],['games','AR','Games'],['music','♫','Music'],['community','CO','Community'],['profile','ID','Profile'],...(!preview?[['merch','MB','Merch'] as const]:[])] as const).map(([id,icon,label])=><button key={id} className={tab===id?'selected':''} aria-current={tab===id?'page':undefined} aria-label={label} onClick={()=>{touchFeedback('tap');setTab(id)}}><span aria-hidden="true">{icon}</span><small>{label}</small></button>)}</nav></div>;
}
 function Login({onUser}:{onUser:(u:User)=>void}){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [signup,setSignup]=useState(false);const [error,setError]=useState('');return <form className="auth-form" aria-label={signup?'Create a BIG CRUISE account':'Sign in to BIG CRUISE'} onSubmit={async e=>{e.preventDefault();setError('');const r=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});if(r.error)setError(r.error.message);else if(r.data.user)onUser({id:r.data.user.id,email:r.data.user.email})}}><label htmlFor="auth-email">Email address</label><input id="auth-email" name="email" type="email" placeholder="you@example.com" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/><label htmlFor="auth-password">Password</label><input id="auth-password" name="password" type="password" placeholder="6+ characters" autoComplete={signup?'new-password':'current-password'} minLength={6} value={password} onChange={e=>setPassword(e.target.value)} required/>{error&&<p className="error" role="alert">{error}</p>}<button className="primary auth-submit" type="submit">{signup?'Create my account':'Enter BIG CRUISE'}</button><button type="button" className="link auth-switch" onClick={()=>{setSignup(!signup);setError('')}}>{signup?'Already have a seat? Sign in':'New here? Create your account'}</button></form>}
createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
